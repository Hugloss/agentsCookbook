# Test Focus P7 Qualification

Phase 7 qualifies bounded verification-ladder selection.

The adversarial corpus proves that confirmed direct owners populate stage 1, confirmed owners of bounded reverse dependents populate stage 2, and explicit caller gates populate stage 3. Naming-only correspondence cannot become direct authority. Changed tests are selected directly; non-Python and deleted/undiscovered changes publish uncertainty rather than guessed tests. Independent impact/test limits preserve omitted evidence, missing broader gates are explicit, repository identity is checkout-independent and invalidates on changed source bytes, escaping paths fail closed, and the common P4 contract validates.

Promotion requires P1 through P7 to pass on the same tree.
